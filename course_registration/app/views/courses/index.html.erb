<h1>All Available Courses</h1><br>
<% @courses.each do |course| %>
  <%= @course.number %> - <%= @course.name %> - <%= link_to "View Course", course_path %>
  <% end %><br>

  <%= link_to "Create a New Course", new_course_path %>
