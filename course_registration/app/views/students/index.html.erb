<h2>Your Students</h2>

<% @students.each do |student| %>
<div>
  <h3><%= student.first_name %> <%= student.last_name%></h3>
  <h4><%= link_to "View Profile", student_path(student.id) %></h4>
<% end %></div>
