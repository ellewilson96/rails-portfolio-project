<%= form_for @grade, url: "/students/#{@grade.student_id}/grades/#{@grade.id}" do |f| %>

  <%= f.label :behavior %>
  <%= f.text_field :behavior %><br>

  <%= f.label :letter_grade %>
  <%= f.text_field :letter_grade %><br>

  <%= f.submit %>
  <% end %>
